#include<stdio.h>
#include<string.h>
int main()
{
	char arr[50],t;
	int i,n;
	printf("请输入一组字符：\n");
	gets(arr);
	n=strlen(arr)-1;
	for(i=0;i<n-i;i++)
	{
		 t=arr[i];
		 arr[i]=arr[n-i];
		 arr[n-i]=t;
	}
	printf("字符倒序：");
	puts(arr);
	return 0;
}
